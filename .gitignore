# Production content - dynamic files only
/payloads/*.json
/article_payloads/*.json
/article_page/*.html

# Keep directory structure but ignore content
!payloads/.gitkeep
!article_payloads/.gitkeep
!article_images/.gitkeep
!article_page/.gitkeep

# Allow static assets that don't change
!/assets/

# NOTE: We now INCLUDE article_images in git since they're compressed (<60KB each)
# and serve as CDN backup

# Backups and temporary files
*.backup
*.tmp
.DS_Store
